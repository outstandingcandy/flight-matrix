# =================================
# AWS Configuration (Unified)
# =================================
aws:
  region: "${AWS_REGION}"                        # AWS region (shared by SES, Bedrock, etc.)
  access_key_id: "${AWS_ACCESS_KEY_ID}"          # Leave empty to use IAM Role
  secret_access_key: "${AWS_SECRET_ACCESS_KEY}"  # Leave empty to use IAM Role
